export interface PrintPDF {
  owner: string;
  billTo: string;
  lot: string;
  killDate: string;
  base: string;
  animal: string;
  rfid: string;
  notes: string;
  weight: string;
  otm: string;
}
